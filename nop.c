#include "monty.h"
/**
 * nop - Monty command that does nothing
 * @stack: pointer to the head of the list representing
 * the stack
 * @line_numer: Instruction (the line number)
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	/** for mandatory Task 5.nop **/
	(void)stack;
	(void)line_number;
}